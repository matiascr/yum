---
version: 2.0.1
title: parser:block_mapping:null_key_with_nested_explicit_mapping_value_test
---
Input:

```yaml
:
  ? nested key
  : nested value
```

-----------------------------------------------------------------------

```gleam
Mapping([Null(Mapping([#(String("nested key"), String("nested value"))]))])

```
